{% comment %}
  Modern Feature Cards Section
  
  A modern store look with clean cards, clear CTAs, and responsive grid.
  Designed for conversion-focused product features and service highlights.
  
  Accepts:
  - heading: {String} Section heading
  - subheading: {String} Optional subheading
  - cards: {Array} Array of feature card objects
{% endcomment %}

{{ 'section-modern-feature-cards.css' | asset_url | stylesheet_tag }}

<section class="modern-feature-cards">
  <div class="modern-feature-cards__wrapper">
    {%- if section.settings.heading != blank or section.settings.subheading != blank -%}
      <div class="modern-feature-cards__header">
        {%- if section.settings.heading != blank -%}
          <h2 class="modern-feature-cards__heading">{{ section.settings.heading }}</h2>
        {%- endif -%}
        
        {%- if section.settings.subheading != blank -%}
          <p class="modern-feature-cards__subheading">{{ section.settings.subheading }}</p>
        {%- endif -%}
      </div>
    {%- endif -%}
    
    {%- if section.blocks.size > 0 -%}
      <div class="modern-feature-cards__grid {% if section.blocks.size > 3 %}modern-feature-cards__grid--many{% endif %}">
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'card' -%}
              <div class="modern-feature-card" {{ block.shopify_attributes }}>
                {%- if block.settings.icon != blank -%}
                  <div class="modern-feature-card__icon" aria-hidden="true">
                    {{ block.settings.icon }}
                  </div>
                {%- endif -%}
                
                {%- if block.settings.title != blank -%}
                  <h3 class="modern-feature-card__title">{{ block.settings.title }}</h3>
                {%- endif -%}
                
                {%- if block.settings.description != blank -%}
                  <p class="modern-feature-card__description">{{ block.settings.description }}</p>
                {%- endif -%}
                
                {%- if block.settings.cta_text != blank and block.settings.cta_link != blank -%}
                  <a href="{{ block.settings.cta_link }}" class="modern-feature-card__cta">
                    {{ block.settings.cta_text }}
                    <span class="modern-feature-card__cta-arrow" aria-hidden="true">→</span>
                  </a>
                {%- endif -%}
              </div>
          {%- endcase -%}
        {%- endfor -%}
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Modern Feature Cards",
  "class": "section",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Título de la sección",
      "default": "Características Premium"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subtítulo (opcional)",
      "default": "Descubre por qué miles de clientes confían en nosotros"
    }
  ],
  "blocks": [
    {
      "type": "card",
      "name": "Tarjeta de Característica",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icono (emoji o símbolo)",
          "default": "✨",
          "info": "Usa emojis como 🚀, 💎, ⚡, 🎯, 🔒, etc."
        },
        {
          "type": "text",
          "id": "title",
          "label": "Título",
          "default": "Característica Destacada"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Descripción",
          "default": "Describe el beneficio clave que obtiene el cliente con esta característica."
        },
        {
          "type": "text",
          "id": "cta_text",
          "label": "Texto del botón",
          "default": "Más información"
        },
        {
          "type": "url",
          "id": "cta_link",
          "label": "Enlace del botón"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Modern Feature Cards",
      "blocks": [
        {
          "type": "card",
          "settings": {
            "icon": "🚀",
            "title": "Envío Ultra Rápido",
            "description": "Recibe tus productos en 2-5 días hábiles en toda Colombia. Procesamos tu pedido el mismo día.",
            "cta_text": "Ver más",
            "cta_link": "shopify://collections/all"
          }
        },
        {
          "type": "card",
          "settings": {
            "icon": "💎",
            "title": "Calidad Premium",
            "description": "Productos seleccionados con los más altos estándares de calidad. Garantía de satisfacción total.",
            "cta_text": "Ver productos",
            "cta_link": "shopify://collections/all"
          }
        },
        {
          "type": "card",
          "settings": {
            "icon": "🔒",
            "title": "Compra Segura",
            "description": "Pago seguro y protegido. Paga cuando recibes tu producto con total confianza.",
            "cta_text": "Saber más",
            "cta_link": "shopify://pages/contacto-techaura"
          }
        }
      ]
    }
  ]
}
{% endschema %}
